# @vben/vsh

用于当前工程的 Shell 工具集合，主要服务于依赖检查、发布检查和工程辅助命令。

## 安装

```bash
pnpm add -D @vben/vsh
```

## 使用

```bash
pnpm vsh [command]
```

## 常用命令

- `vsh check-deps`
- `vsh scan-circular`
- `vsh publish-check`
