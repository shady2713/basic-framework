/**
 * system API 包，定义并实现提供给其它模块的 API。
 * 其中 CommonApi 契约与 DTO 由 basic-framework-module-system-api 薄模块承载，
 * 供 framework 下的 starter 与其它业务模块依赖（只允许依赖契约，不得触碰实现）。
 */
package com.basicframework.module.system.api;
